const assert = require('assert');
const exchanges = require ('./exchanges');

describe('exchanges', function() {
  
  describe('#fetchPositiveBalances()', function() {
    it('should fetchPositiveBalances', function() {
      assert.equal([1,2,3].indexOf(4), -1);
    });
  });

});